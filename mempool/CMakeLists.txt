include_directories(src)
set(dir output)
file(MAKE_DIRECTORY ${dir})
file(MAKE_DIRECTORY ${dir}/test)
add_executable(test/TestMempool example/TestMempool.cpp)
target_link_libraries(test/TestMempool gflags)